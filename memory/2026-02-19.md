# 2026-02-19 - 工作日志

## OpenClaw Config GUI 项目 (端口 8091)

### 已完成功能
- 后端 API (`server.js`):
  - `GET/POST /api/config` - 读取/保存配置
  - `POST /api/config/validate` - 验证配置
  - `GET /api/backups` - 获取备份列表
  - `POST /api/restore` - 恢复备份
  - `GET /api/nodes` - 获取节点列表
  - `POST /api/nodes/register` - 注册远程节点
  - `DELETE /api/nodes/:id` - 删除节点
  
- 前端功能 (`app.js`, `index.html`):
  - 自动加载本地配置
  - 全局节点切换器 (下拉菜单切换本地/远程配置)
  - 保存/验证按钮
  - 备份管理 UI
  - 模型列表显示为表格格式 (非折叠卡片)
  - 配置段复制/粘贴功能 (多选复制，覆盖粘贴)

- 远程 Agent:
  - 端口 18790
  - Token: `ur6e04hw07sl0l5jz4z2mobd0wjohaem`
  - 自动注册: `install.sh --manager-url --name`

### 关键决策
- **原子写入**: 使用 `.tmp` 文件 + `fs.renameSync()` 防止损坏
- **服务端验证**: `validateConfig()` 检查类型
- **节点存储**: `/root/.openclaw/registered-nodes.json`
- **备份保留**: 最后 20 个备份

## AxonHub 项目

### 路径
- 项目根: `/root/axonhub/`
- 前端: `/root/axonhub/frontend/` (React + Vite + TypeScript + Tailwind)
- 后端: Go (GraphQL API)
- Landing Page: `http://localhost:5173/landing`

### Landing Page (已完成)
- 路径: `/root/axonhub/frontend/src/features/landing/`
- 组件: Hero, Features, HowItWorks, CodeExample, Pricing, CTA, Footer, Navbar
- 主题: 深色 (#0a0a0a) + 翠绿强调色 (#10b981)
- 动画: Framer Motion
- 路由: `/landing` (公开路由)

### 认证架构分析

#### 现有 Go 后端认证 (`/internal/server/biz/auth.go`)
- JWT Token 生成 (自定义实现)
- Secret Key 存储在 system 表 (`SystemKeySecretKey`)
- 支持 API Key 和 JWT Token 两种认证方式
- 中间件: `/internal/server/middleware/auth.go`

#### 前端认证
- API 客户端: `/root/axonhub/frontend/src/features/auth/data/auth.ts`
- 登录表单: `sign-in/index.tsx`, `user-auth-form.tsx`
- 使用 `authApi.signIn` 方法

### Better Auth 集成 (进行中)
- 路径: `/root/axonhub/auth-service/`
- 依赖: better-auth, better-auth-hono-adapter, drizzle-orm, better-sqlite3
- 功能: Email/Password, 2FA, Passkey, Admin, Multi-session

#### 待决策 - 统一认证架构方案
1. **方案1**: 将 Better Auth 集成到 Go 后端
2. **方案2**: 增强现有 Go 认证以支持 OAuth
3. **方案3**: 保持 Better Auth 独立，与 Go 后端共享 JWT Secret

**关键挑战**: 
- 添加 OAuth 社交登录
- 保持现有 JWT 认证兼容性
- 前端 `authApi.signIn` 兼容

## Claude Code 调度系统

### 路径
- 调度脚本: `/root/.openclaw/workspace/claude-code-dispatch/scripts/dispatch.sh`
- 结果目录: `/root/.openclaw/data/claude-code-results/`

### 关键配置
- 必须使用 `bash -lc` 或显式设置 PATH
- 使用 `--permission-mode acceptEdits` (非 `--dangerously-skip-permissions`)
- 需要 PTY 环境

## 其他

### Claude Code Hooks
- 已安装到 `~/.claude/hooks/notify-agi.sh`
- 来自 `https://github.com/win4r/claude-code-hooks`

### OpenClaw 版本
- 已升级到 v2026.2.17
- 配置文件: `/root/.openclaw/openclaw.json`
- 备份目录: `/root/.openclaw/config-backups/`

---

## 晚间更新 (22:32)

### OAuth 集成 ✅ 完成
- **方案选择**: 方案3 - 保持 Better Auth 独立服务，与 Go 后端共享 JWT Secret
- **OAuth 提供商**: Google, GitHub
- **认证流程**: 分离的 GET 端点发起 OAuth，回调处理 token 交换并重定向到前端 (JWT 在 URL 中)
- **服务端口**: Auth Service 3001

### Dashboard 首页重设计 ✅
- 现代、简洁、专业、科技感
- 动画: Framer Motion (0.1s 递增延迟)
- 悬停效果、背景动画

### Landing Page 视觉增强 ✅
- 粒子效果、发光球体、动画
- 文档: `/root/axonhub/IMPLEMENTATION_COMPLETE.md`, `/root/axonhub/VISUAL_GUIDE.md`

### Provider Logos 修复 ✅
- **问题**: 外部 CDN 图片不可靠导致 broken images
- **解决方案**: 使用内嵌 SVG 图标替代外部 URL
- **文件**: `/root/axonhub/frontend/src/features/landing/components/providers.tsx`
- **支持的 Provider**: OpenAI, Anthropic, Google, Azure, Meta, DeepSeek, Cohere, Mistral

### Muskapi.com 品牌重塑 (进行中)
- **新域名**: Muskapi.com
- **更新统计**: "20+ AI 服务商", "100+ 支持模型"
- **待更新**: Hero 组件品牌信息
- **待创建**: 定价计划页面
- **待集成**: 用户管理系统、支付系统

### 关键配置
- **Vite 外部访问**: 必须使用 `--host 0.0.0.0` (不能只用 localhost)
- **前端访问**: `http://122.10.119.154:5173`
- **Landing Page**: `http://122.10.119.154:5173/landing`
- **Dashboard**: `http://122.10.119.154:5173/` (需要登录)

### 支付集成 (待办)
- **Stripe**: 国际信用卡支付
- **EPUSDT**: USDT 加密货币支付
- **Cloudflare Zone**: claudeai.best (用于 EPUSDT 域名配置)

### 服务状态
- Auth Service: 端口 3001 ✅
- Frontend: 端口 5173 ✅
- 依赖安装: 1047 packages ✅

---

## 下一步 (更新)
1. ~~决定 AxonHub 统一认证架构方案~~ ✅ (方案3)
2. ~~实现选定的认证方案~~ ✅ (OAuth 完成)
3. ~~测试 AxonHub Landing Page~~ ✅
4. 更新 Hero 组件为 Muskapi.com 品牌
5. 创建定价计划组件
6. 集成用户管理系统到前端
7. 创建默认管理员用户 (测试用)
8. 集成 Stripe 支付
9. 集成 EPUSDT 支付
10. 配置 Cloudflare DNS (claudeai.best zone)

---

## 深夜更新 (23:44)

### CCCC 协作框架分析进展 ✅
- 新增目标: 分析并本地部署 ，用于 Claude Code + Codex 协作开发
- 仓库路径: 
- 架构确认: Python 后端 (daemon/kernel/CLI) + React/Vite Web UI + Docker 支持
- 运行时支持:  与  双运行时，含 MCP 自动配置能力
- 核心数据模型:  目录， 为 append-only 事件日志，daemon 为单写入者

### CCCC 核心机制（已验证）
- Group 生命周期为多 actor 协作核心； 负责配置校验、runner 拉起、状态事件发射
- 请求路由通过  分发到各 ops handler
- 基础协作流程: 
- 默认 Web UI: 

### 已完成阅读的关键文件
- CLI: 
- Daemon: , 
- Dispatch/Lifecycle: , 
- Kernel: , , , , 
- Runners: , 
- 其他: , , 

### 环境/执行层阻塞（待排查）
- 本地服务曾反复出现 （影响 Vite 与 auth-service 稳定性）
- 工具可用性缺口:  缺失、Brave 搜索 429、 不可用

### 下一阶段执行重点
1. 深入完成  细节核对（start/stop 全链路）
2. 产出 CCCC 本地部署方案（含 Claude/Codex runtime 配置模板）
3. 在 AxonHub 侧继续 Muskapi.com 品牌改造（Hero/Pricing/支付与用户管理）


---

## 深夜更新补录 (23:44)

### 记录修正
- 补录说明: 上一条“深夜更新 (23:44)”存在转义导致的字段丢失，本条补齐完整关键信息。

### CCCC 项目状态（已确认）
- 代码仓库: 
- 本地路径: 
- 架构: Python backend (daemon/kernel/CLI) + React/Vite web + Docker
- 运行时: 支持  与 ，并包含 MCP 自动配置相关能力
- 核心存储: ，事件日志为 append-only ，daemon 负责单写入
- Web UI 默认地址: 

### 核心机制理解（阶段性结论）
- 请求分发:  负责将请求路由到具体 ops handler。
- 生命周期: group 为多 actor 协作核心， 执行配置校验、runner 拉起与状态事件发射。
- 基础流程: 。

### 已重点阅读源码
- CLI: 
- Daemon: , 
- Dispatch/Lifecycle: , 
- Kernel: , , , , 
- Runners: , 
- MCP/Docker: , , 

### 未完成项与后续
1. 完成  的 start/stop 全链路核对。
2. 产出 CCCC 本地部署与 Claude/Codex 协作配置方案。
3. 并行推进 Muskapi.com 改造：Hero/Pricing、用户管理、Stripe + EPUSDT。


---

## 深夜补充记录 (23:44, canonical)
- 修正说明: 之前两条 23:44 记录因命令转义导致部分字段为空，本条作为完整有效记录。
- CCCC 仓库: https://github.com/ChesterRa/cccc/
- CCCC 本地目录: /root/.openclaw/workspace/cccc/
- CCCC 架构: Python backend (daemon/kernel/CLI) + React/Vite Web UI + Docker。
- CCCC 运行时: 同时支持 claude 与 codex，包含 MCP 自动配置逻辑。
- CCCC 数据模型: CCCC_HOME=~/.cccc，ledger.jsonl 为 append-only 事件日志，daemon 负责单写入。
- CCCC 默认 Web UI: 127.0.0.1:8848。
- 已读核心文件: src/cccc/cli.py, src/cccc/daemon_main.py, src/cccc/daemon/server.py, src/cccc/daemon/request_dispatch_ops.py, src/cccc/daemon/ops/group_lifecycle_ops.py, src/cccc/kernel/actors.py, src/cccc/kernel/group.py, src/cccc/kernel/ledger.py, src/cccc/kernel/registry.py, src/cccc/kernel/runtime.py, src/cccc/runners/pty.py, src/cccc/runners/headless.py, src/cccc/daemon/mcp_install.py, docker/Dockerfile, docker/docker-compose.yml。
- 阶段结论: group_start 负责配置校验、runner 启动与状态事件发射；request_dispatch_ops 负责请求分发；协作流程为 attach -> setup(runtime) -> actor add -> group start -> send。
- 当前阻塞: 环境出现 Exec failed (signal SIGKILL) 影响 Vite/auth-service 稳定性；rg 缺失；Brave 搜索限流 429；memory_search 工具不可用。
- 后续优先级: 1) 完成 group_lifecycle_ops start/stop 全链路分析；2) 产出 CCCC 本地部署与 Claude/Codex 协作配置方案；3) 继续 Muskapi.com Hero/Pricing、用户管理、Stripe + EPUSDT 落地。
